- [Welcome endpoint concurrency](welcome-endpoint-concurrency.md) — auto-welcome generation needs a pg advisory lock + post-generation recheck or React StrictMode double-mounts produce duplicate greetings.
- [Locking transient query data into UI state](lock-query-data-into-state.md) — when an overlay must show one snapshot of a query result and ignore later refetches, lock it in a useEffect that gates on `!locked && incoming`, never via ref-mutation during render.
- [No-flash route → overlay transition](no-flash-overlay-transition.md) — for an instant fullscreen loader on a route, render it from the destination route with `useState(true)` initial, no data-gate, fully-painted background, and don't await `useQuery` results before mounting it.
- [Living-system ambient motion](living-system-ambient-motion.md) — make a UI feel "alive but restrained" by stacking several slow low-amplitude CSS keyframe loops (breath, drift, pulse) with desynchronized periods/phases and tiny opacity/scale deltas — not RAF, not big animations.
- [Attachment storage tradeoff](attachment-storage-tradeoff.md) — for personal-tool uploads: disk + DB metadata + base64 JSON wire (no multipart, no GCS), and only inline image bytes for the latest user turn in the LLM payload — older turns become text placeholders.
