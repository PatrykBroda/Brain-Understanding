{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "vitest run",
    "test:api-server": "vitest run artifacts/api-server/src/__tests__/",
    "test:mobile-smoke": "bash scripts/run-mobile-smoke.sh",
    "test:coach-smoke": "playwright test e2e/coach-smoke.spec.ts"
  },
  "private": true,
  "devDependencies": {
    "@clerk/testing": "^2.1.4",
    "@playwright/test": "^1.61.0",
    "@types/pg": "^8.20.0",
    "pg": "^8.20.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3",
    "vitest": "^4.1.9"
  }
}
